"use client";
import React from "react";

const Web = () => (
  <>
    <div
      onClick={() => currentSlidefeeds(2)}
      className="banner push-color feed-buttons"
    >
      <h4> Web </h4>
    </div>
  </>
);

export default Web;
